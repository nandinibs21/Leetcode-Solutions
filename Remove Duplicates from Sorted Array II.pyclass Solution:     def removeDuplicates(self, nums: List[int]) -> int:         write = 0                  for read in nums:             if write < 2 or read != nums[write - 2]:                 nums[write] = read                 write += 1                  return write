class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        write = 0
        
        for read in nums:
            if write < 2 or read != nums[write - 2]:
                nums[write] = read
                write += 1
        
        return write
